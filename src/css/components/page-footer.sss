.page-footer

  & hr
    border: 0
    height: 1px
    background-image:
      linear-gradient(
        to right,
        color-mod(var(--hr-gradient) alpha(0)),
        color-mod(var(--hr-gradient) alpha(0.75)),
        color-mod(var(--hr-gradient) alpha(0))
      )

  & .team2767
    height: 4rem
